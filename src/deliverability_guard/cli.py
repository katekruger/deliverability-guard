"""Command-line entry point.

Not yet implemented. The breaker engine (engine/breaker.py), provider drivers
(providers/), and the two-loop controller (loops/) land in later build-plan
prompts; this module wires them into a CLI once they exist. See
BUILD-PLAN.md §10 for the intended shape.
"""
